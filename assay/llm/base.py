"""LLMProvider interface — narration ONLY, never touches the detection path.

TODO(S9): `LLMProvider` protocol with `narrate(report_json: dict) -> str`.
"""

from __future__ import annotations

# TODO(S9): class LLMProvider(Protocol): def narrate(self, report_json: dict) -> str: ...
